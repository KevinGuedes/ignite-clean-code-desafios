function lookForUpdades() {}

const INTERVAL_OF_30_MINUTES_IN_MS = 60 * 30 * 1000
setInterval(lookForUpdades, INTERVAL_OF_30_MINUTES_IN_MS)

function calculateDiscount(priceInCents, discountInPercentage) {}
